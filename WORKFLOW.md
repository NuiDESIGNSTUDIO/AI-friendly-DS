# WORKFLOW.md — 思考ステップ手順書

UI/デザインタスクに着手する際は、必ず以下の Step0〜Step6 の順に進めること。
**Step1が完了し人間の合意を得るまで、コードは一切書かない。**

## Step 0: 現行確認
- 現行画面の把握は次の優先順位で行う。`mock/` に対象画面が無ければ②③のみで進める。
  1. `mock/` — 過去にfixした最新のデザイン意図(実装より新しい場合がある)
  2. STG環境 / Figma — 見た目の確認
  3. `reference/sharewis-act/`(本番サイトのローカル参照クローン)のコードを実際に読み、現行画面の実装・構成・使用コンポーネントを把握する(実装の裏付け)
- `components.json`(Figmaコンポーネント一覧)と `icons.json`(アイコン一覧)を検索し、使用できる部品・アイコンとそのFigma node-idを洗い出す。該当コンポーネントが見つかったら、Figma MCPの `get_design_context`(コンポーネント)/`download_assets`(アイコンSVG)でnode-idを指定し、実際のデザイン・コードを取得する。
- `screens.json`(人間デザイナー作成の参考画面カタログ)を検索し、対象画面と役割が近いものがあれば、レイアウト・情報設計・項目構成の参考にする(`assets/reference-screens/`のスクリーンショット、または`get_design_context`でnode-idを直接参照)。各screens.jsonエントリは人間が指定した代表フレームであり、そのまま丸ごと複製する対象ではなく「参考にする画面パターン」として扱う。
- `tokens.json`（色・余白）と `rules.json`（禁止パターン）を読み込む。
- 要件・対象範囲に不明点がある場合は、憶測で進めず1問ずつ質問して解消してからStep1に進む(まとめて全部聞くより、1問ずつの方が回答しやすい)。

## Step 1: 3パターンの文章提案（コード化禁止）
方向性の異なる3案を、コードを書かずに文章／簡易構造で提示する。似た案を3つ出さず、判断軸が明確に異なる3案にする。

**デフォルトの軸**(特に理由がなければこれを使う):
- **A案（標準・保守）**: 既存STGデザインを踏襲した、迷わせない堅実な配置
- **B案（シンプル・ミニマル）**: 余白を広めに取り、要素数を絞った配置
- **C案（機能・情報強調）**: 主要な操作導線・重要情報を最優先で目立たせる配置

案件の文脈(画面の種類・要件・Step0で把握した実装制約・過去のfeedback-log.md)によってこのデフォルト軸が噛み合わない場合は、「無難/攻め/中間」のような機械的な当てはめにせず、その案件に本当に意味のある3つの軸を設計してよい。軸を変更した場合は、なぜこの3軸にしたかを一言添えて提示する(例: 「今回は一覧画面のため、情報密度の異なる3案(高密度・標準・低密度)で比較します」)。

各案は次の簡易テンプレートで提示する(判断材料を揃え、比較しやすくするため)。
```
## A案(標準・保守)
- 一言で言うと: ...
- 配置・余白の方向性: ...
- この案が向いている理由: ...
- 注意点・トレードオフ: ...
```

## Step 2: 人間との出し戻し・方向性決定
- 人間からのフィードバックを受け、方向性を確定する（例: 「B案ベースにA案の◯◯を混ぜる」）。
- 提案の直後には次の形式で選択を促す。
  ```
  どれに近いですか?
  A案に近い / B案に近い / C案に近い / A案とC案を混ぜたい / どれも違う
  (「どれも違う」場合は「もっと堅く」「もっと余白広く」など感覚語でもOK)
  ```
- 合意が取れるまでコード生成に進まない。

## Step 3: 実装 & 自動検品
- `drafts/YYYY-MM-DD_タスク概要/` を新規作成し、決定した方向性に沿ってプロトタイプの静的HTML(+CSS)をそこに出力する。特定のフレームワーク(React/Rails等の本番実装先スタック)への最適化は行わない。
- `tokens.json` の値・既存コンポーネントを優先利用し、直書きを避ける。
- Edit/Write 実行後、`.claude/settings.json` の PostToolUse Hook が `scripts/hook-check-rule.sh` を自動実行し `rules.json` 違反を検知する。
- 違反が検知された場合、人間に見せる前に自分で修正する。

## Step 4: セルフレビュー
- `design-review` スキルを実行し、レイアウトバランス・余白・トーン&マナー・アクセシビリティを最終確認する。Critical/High相当は人間に見せる前に自分で直す。
- 人間に提示する際は、コード(HTML)だけでなく判断材料も揃えて出す: 何を調べたか(Step0)、どの案をベースにした理由(Step1/2)、レビューで直した点(Step4)を短くまとめて添える。見た目だけを渡すと「これは何のための画面か」の往復が発生するため、仕様まで含めてレビューできる状態にする。

## Step 5: ブラウザプレビュー & 反復修正
- Step3で生成したHTMLを、コードのまま見せるのではなく Browserペイン(`mcp__Claude_Browser__*`)で実際にレンダリングして見せる。人間が操作するブラウザではなく、会話画面内に表示されるAI制御のブラウザペインで確認する運用とする。
- Step4のまとめ(何を調べたか・選定理由・直した点)を添えて画面を提示し、フィードバックを受ける。
- フィードバックに沿ってHTMLを修正 → Browserペインで再描画 → 確認、を1往復とする。**目安は1依頼につき3〜4往復。** 想定より往復が伸びる場合は、方向性の前提(Step1/2)から見直す。
- 修正のたびにEdit/Writeが走るため、`scripts/hook-check-rule.sh` による自動検品は各往復で自動的にかかる。
- 同種の指摘が繰り返される場合は、その場で `ban-pattern` スキルを使って `rules.json` と `feedback-log.md` に反映し、次回以降AI自身が同じ指摘を避けられるようにする。
- 人間の最終OK(fix)が出た時点のHTMLファイルが完成品。追加の変換作業なしでそのまま実装に使える形とする。

## Step 6: mockへの反映
- fixしたHTMLを `mock/admins/<controller>/<action>.html` へコピー/上書きする(命名は `reference/sharewis-act` の `app/views/admins/<controller>/<action>` に揃える。詳細は `mock/README.md` 参照)。
- 新規画面で対応するcontrollerが実装側にまだ存在しない場合は、Figmaの構成を踏まえて妥当なパスを起こしてよい。迷ったら人間に確認する。
- `drafts/` 配下の検討ファイルは削除せず残す(3案の検討履歴として)。
- **AIの作業範囲はここまで。** `reference/sharewis-act`(実装リポジトリ)への反映・移植は人間(エンジニア)が行う。AIは実装リポジトリへの書き込み・コミットを行わない。
